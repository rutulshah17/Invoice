<% include ../partials/header.ejs %>
<% include ../partials/modal.ejs %>


<div class="modal hide" id="addBookDialog">
    <div class="modal-header">
        <button class="close" data-dismiss="modal">×</button>
        <h3>Modal header</h3>
    </div>
    <div class="modal-body">
        <p>some content</p>
        <input type="text" name="bookId" id="bookId" value=""/>
    </div>
</div>




<table class="table table-striped table-hover">
  <tr>
    <th>Name Of Client</th>
    <th>Amount</th>
    <th>Date From</th>
    <th>Date To</th>
      <th></th>
      <th></th>
  </tr>

    <% for (var i = 0; i < invoices.length; i++) { %>
      <tr>
        <td><%= invoices[i].nameOfClient %></td>
        <td>$<%= invoices[i].amount %></td>
        <td><%= invoices[i].dateFrom.getUTCDate()%>-<%=invoices[i].dateFrom.getUTCMonth()+1%>-<%=invoices[i].dateFrom.getUTCFullYear()%></td>
        <td><%= invoices[i].dateTo.getUTCDate()%>-<%=invoices[i].dateTo.getUTCMonth()+1%>-<%=invoices[i].dateTo.getUTCFullYear()%>%></td>
          <td><a href="#" class="btn btn-primary">Edit</a></td>
          <td><a href="#deleteRecord" data-toggle="modal" data-delete-modal-id=<%= invoices[i]._id %> data-target="#deleteRecord" class="btn btn-warning">Delete</a></td>
      </tr>
    <% } %>
</table>
<% include ../partials/footer.ejs %>